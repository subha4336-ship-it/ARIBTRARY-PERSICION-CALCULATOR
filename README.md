# ARIBTRARY-PERSICION-CALCULATOR
This project implements an Arbitrary Precision Calculator (APC) in C that performs arithmetic operations on integers of any size using doubly and singly linked lists, handling signs, carry, borrow, and division without using built-in big integer libraries.
